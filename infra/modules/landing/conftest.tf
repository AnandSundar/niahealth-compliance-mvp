###############################################################################
# modules/landing/conftest.tf
#
# Placeholder for module-local OPA/Conftest fixtures (e.g. a
# sample.tf.json to drive local conftest runs while developing the
# module). The actual OPA policy lives at
# infra/policies/conftest.rego and is enforced at the root of the
# infra/ tree.
#
# Keep this file present (and empty) so a later unit can drop in
# fixtures without re-shuffling the file list.
###############################################################################
