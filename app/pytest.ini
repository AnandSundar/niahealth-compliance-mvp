###############################################################################
# pytest.ini
# Tells pytest where the rootdir is and that the testpaths is tests/.
# This lets `pytest -v` be run from the app/ directory without
# touching the repo root.
###############################################################################

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
